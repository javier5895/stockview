{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/main" }
  ],
  "functions": {
    "api/main.py": {
      "runtime": "python3.11",
      "maxDuration": 60
    }
  }
}
